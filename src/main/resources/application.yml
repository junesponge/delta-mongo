spring:
  application:
    name: delta-mongo
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test

server:
  servlet:
    context-path: /api
